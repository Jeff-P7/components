import { TColor, ColorTypes } from '../../color';
import { TOpacity, OpacityTypes } from '../../opacity';

export const PlaceholderColorTypes: Array<string> = ColorTypes;
export type TPlaceholderColor = TColor;

export const PlaceholderOpacityTypes: Array<number> = OpacityTypes;
export type TPlaceholderOpacity = TOpacity;
